# The desktop picker. XFCE is already on the ISO and is ticked and locked, so
# every install has a working desktop even with no network. GNOME and KDE are
# optional and are downloaded at this point, which is why they are marked as
# needing a connection.
---
label:
    sidebar: "Desktop"
    title: "Choose your desktop"

groupsFilePath: /etc/calamares/netinstall-desktops.yaml
required: false
